version: '3.8'

services:
  db:
    container_name: pgsql
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: test_db
    ports:
      - "5432:5432"
  pgadmin:
    container_name: pgadmin4
    image: dpage/pgadmin4
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: root
    ports:
      - "5050:80"
  secretmessage:
    container_name: secretmessage-go
    build:
      context: .
      dockerfile: Dockerfile-secretmessage
    ports:
      - "8080:8080"
    depends_on:
      - db
